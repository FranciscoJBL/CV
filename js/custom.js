var change= false;
$(window).scroll(function(){
    window_y = $(window).scrollTop(); 
    scroll_critical = parseInt($("#PRIMER_DIV").height()); 
    if (window_y > scroll_critical) { 
      
    } else {

    }
});